# Roteiro

Painel de acompanhamento de rotas de caminhão, stages, taikis e horários.

## Rodar localmente

```bash
npm install
npm run dev
```

Abra `http://localhost:3000`.

Os dados são salvos no `localStorage` do navegador.
